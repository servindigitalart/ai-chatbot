version: '3.8'
services:
  ai-chatbot:
    build: .
    ports:
      - "8005:8005"
    env_file: .env
    volumes:
      - .:/app
    command: uvicorn api.main:app --host 0.0.0.0 --port 8005 --reload
